Bibliotecas que precisam ser instaladas através do pip install para o pleno funcionamento do programa

python -m pip install py-cpuinfo
python -m pip install psutil
python -m pip install mysql-connector-python
python -m pip install matplot
python -m pip install dashing
python -m pip install openpyxl

python -m venv venv
cd venv/
source bin/activate
cd ..

pip install py-cpuinfo && pip install psutil && pip install mysql-connector-python && pip install matplot && pip install dashing && pip install openpyxl

